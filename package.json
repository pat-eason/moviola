{
  "name": "moviola",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "On-device video -> (frame + transcript) digest for Claude Code, via ffmpeg + Parakeet/whisper.cpp.",
  "scripts": {
    "doctor": "tsx scripts/doctor.ts",
    "setup": "tsx scripts/setup.ts",
    "digest": "tsx scripts/digest.ts"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "tsx": "^4.0.0"
  }
}
